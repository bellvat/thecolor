<% @movies.each do |m| %>
  <div class="box col-10 offset-1">
    <h2><%= m.movie %> </h2>
    <h5><%=m.month %>
    <%= m.year %>
    </h5>
    <p>Thoughts: <%= m.thoughts %></p>

  </div>

<% end %>

<%= will_paginate @movies, class:'apple_pagination' %>