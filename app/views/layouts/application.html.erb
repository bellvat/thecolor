<!DOCTYPE html>
<html>
  <head>
    <title>Thecolor</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="row row1">
      <div class="text-right col-6">
        <%= link_to root_path, class:'title' do %>
        thecolor
        <%end%>
      </div>
      <div class="text-right col-5 add_placement">

          <%= link_to '-add-','#', data: {toggle: 'modal', target: '#myModal'}, class: 'add'%>
        </div>
      

      </div>

    </header>

    <div class="blurb col text-center">
      <p> A memory place for films you watched </p>
      <p> and how they made you feel. </p>
    </div>



    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h4 class="modal-title" id="myModalLabel">Add a Movie!</h4>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
            
          </div>

          <%= simple_form_for Movie.new, defaults: {input_html:{ class: 'form-control'}} do |f| %>
            <div class="modal-body">
              <%= f.input :movie, label: "What is the movie you want to add?" %>
              <br />
              <%= f.input :month, label: "What month did you watch it?" %>
              <br />
              <%= f.input :year, label: "What year did you watch it?" %>
              <br />
              <%= f.input :thoughts, label: "What are some of your thoughts on it?" %>
              <br />
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <%= f.submit 'Create' , class: 'btn btn-primary' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>


    <%= yield %>

  </body>
</html>
