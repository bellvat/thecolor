<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Thecolor</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="row row1">
      <div class="text-center col-7 offset-1">
        <%= link_to root_path, class:'title' do %>
         thecolor
        <%end%>
      </div>
      <div class="text-center col-3 offset-1 add_placement">
          <%= link_to '-add-','#', data: {toggle: 'modal', target: '#myModal'}, class: 'add'%>
        </div>
      </div>

    </header>

    <div class="blurb col-7 offset-1 text-center">
      <p> Your film collection. </p>
    </div>


    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h4 class="modal-title" id="myModalLabel">Add a Movie!</h4>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
            
          </div>

          <%= simple_form_for Movie.new, defaults: {input_html:{ class: 'form-control'}} do |f| %>
            <div class="modal-body">
              <%= f.input :movie, label: "Movie title:" %>
              <br />
              <%= f.input :year, as: :date, start_year: Date.today.year, end_year: Date.today.year-100, discard_day: true, order: [:month, :year], include_blank: true, default: nil, :input_html => {:class => 'profile-date'},label: "When did you watch it?" %>
              <br />
              <%= f.input :thoughts, label: "Your thoughts:" %>
              <br />
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <%= f.submit 'Create' , class: 'btn btn-primary' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>




    <%= yield %>

  </body>
</html>
